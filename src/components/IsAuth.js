import React from "react";
import { Formik, Form, Field, useFormik } from "formik";
import * as Yup from "yup";
import axios, { Axios } from "axios";

export default function IsAuth() {
  /*
  check if local storage has a token and if that token matches the user credentials API post. 
  */
  /*
    Como hacer que muchas paginas distintas puedan saber si el usuario actual esta logeado o no.
  */
}
